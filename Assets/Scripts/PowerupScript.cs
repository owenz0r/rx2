using UnityEngine;
using System.Collections;

public class PowerupScript : MonoBehaviour {

	/*
	public string type;
	public Sprite triShotSprite;
	public Vector3 spriteScale;
	*/

	// Use this for initialization
	void Start () {
		/*
		if( type == "trishot" )
		{
			GameObject child = Instantiate ( new GameObject() ) as GameObject;
			Destroy( child, 60.0f );
			child.transform.parent = this.transform;
			child.transform.localScale = spriteScale;
			SpriteRenderer sp = child.AddComponent( "SpriteRenderer" ) as SpriteRenderer;
			sp.sprite = triShotSprite;
		}
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
